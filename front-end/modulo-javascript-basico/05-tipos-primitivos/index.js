let nome = "Ulisses"; //string == texto\

let numero = 0.66666; //tipo inteiro == numeros

let usuarioVerificado = false; //tipo booleano == verdadeiro ou falso

let pessoa = null; //tipo null == não armazena nada na variavel ou um dado invalido.

let telefone = undefined; //tipo indefinido == normalmente é um dado que pode ser recebido no javascript e não utilizado no código.
